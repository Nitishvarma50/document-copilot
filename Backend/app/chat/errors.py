"""Safe errors shared by persistence, HTTP routes, and SSE orchestration."""


class ChatError(Exception):
    def __init__(self, status_code: int, detail: str) -> None:
        super().__init__(detail)
        self.status_code = status_code
        self.detail = detail
